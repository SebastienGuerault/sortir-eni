<form method="post" action="{{ path('campus_supprimer', {'id': campus.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete'~campus.id) }}">
    <button class="btn">Supprimer</button>
</form>