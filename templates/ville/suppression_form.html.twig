<form method="post" action="{{ path('ville_supprimer', {'id': ville.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
    {# <input type="hidden" name="_token" value="{{ csrf_token('delete'~ville.id) }}"> #}
    <button class="btn">Supprimer</button>
</form>