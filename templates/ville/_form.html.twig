{{ form_start(form) }}
{# {{ form_widget(form) }} #}

<div class="form_alignement">{{ form_row(form.nom) }}</div>

<div class="form_alignement">{{ form_row(form.codePostal) }}</div>
<br>





<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}