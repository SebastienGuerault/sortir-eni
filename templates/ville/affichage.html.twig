
{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ ville.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ ville.nom }}</td>
        </tr>
        <tr>
            <th>codePostal</th>
            <td>{{ ville.codePostal }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('ville_liste') }}">Retour à la liste</a>

    <a href="{{ path('ville_modifier', {'id': ville.id}) }}"  class="button" >Modifier</a>

    {{ include('ville/suppression_form.html.twig') }}
{% endblock %}